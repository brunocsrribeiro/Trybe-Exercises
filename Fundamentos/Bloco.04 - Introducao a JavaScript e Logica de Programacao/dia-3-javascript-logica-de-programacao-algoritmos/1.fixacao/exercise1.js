let lines = 5;
let symbols = "*";
let inputLine = "";

for (let idx = 0; idx < lines; idx += 1) {
  inputLine += symbols;
}
for (let lineIdx = 0; lineIdx < lines; lineIdx += 1) {
  console.log(inputLine);
}
